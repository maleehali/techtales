const loginFormHandler = async (event) => {
    event.preventDefault();
    // Code for login form handling
  };
  document.querySelector('#login-form').addEventListener('submit', loginFormHandler);
  